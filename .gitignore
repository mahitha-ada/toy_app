# dependencies
node_modules/

# downloaded model weights (fetched on first run, cached locally)
.cache/

# vendored browser libs — regenerated by `npm install` (postinstall)
public/vendor/

# logs / os
*.log
.DS_Store
